name: Cloudflare Pages only

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  post-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Register Telegram webhook on Cloudflare
        run: |
          curl -fsS "https://api.telegram.org/bot8940120225:AAHONShsV1iwDRYIiqciNovNQoB4OyvFqhQ/setWebhook" \
            -d "url=https://beijing-puffmarket.pages.dev/bot-webhook"
